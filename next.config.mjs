/** @type {import('next').NextConfig} */
const nextConfig = {
  /* images: {
    unoptimized: true,
  }, */

   images: {
    domains: [
      'http://backend.almunifi.com',
     
    ],
  },

};

export default nextConfig;
